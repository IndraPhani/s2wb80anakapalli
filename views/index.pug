extends layout

block content
  h1= title
  p Welcome to #{title}
  p i love superheros
  br
  - var btnType = 'success'
  - var btnSize = 'xl'
  button(type='button' class='btn btn-' + btnType + ' btn-' + btnSize) press me
